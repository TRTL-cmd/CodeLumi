# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build / output
dist/
dist-electron/
release/
build/
/.parcel-cache/

# OS files
.DS_Store
Thumbs.db

# Python virtualenv
.venv/
venv/

# ═══════════════════════════════════════════════════════════════
# LUMI DATA ORGANIZATION (after path restructuring)
# ═══════════════════════════════════════════════════════════════
#
# PROJECT DATA (tracked, shareable):
#   archives/          - Session archives
#   training/          - Knowledge base and training log
#   staging.jsonl      - Suggestions
#   userData/backups/  - Code backups
#
# PRIVATE DATA (NOT tracked, in AppData):
#   %APPDATA%\lumi-desktop\lumi_memory.jsonl
#   %APPDATA%\lumi-desktop\selflearn_config.json
#   %APPDATA%\lumi-desktop\selflearn_progress.json
# ═══════════════════════════════════════════════════════════════

# User data directory (mostly tracked, but exclude sensitive files if present)
userData/logs/
# Exclude sensitive files if accidentally placed in project
userData/lumi_memory.jsonl
userData/selflearn_config.json
userData/selflearn_progress.json

# Training backups
training/*.backup*
training/*.bak
training/*.backup.redact*

# IDE
.vscode/
.idea/

# Environment
.env
.env.local

# Logs
*.log

# Keep .husky/ committed so hooks are part of the repo when initialized
.venv/
node_modules/
dist/
.DS_Store
.env
build/
release/

# User data (specific exclusions)
userData/logs/
userData/*.log

# Privacy tool backups
*.backup
